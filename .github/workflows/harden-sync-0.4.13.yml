name: Legacy sync hardening 0.4.13 (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy sync patch workflow retired. Synchronization hardening is already incorporated into the branch."
