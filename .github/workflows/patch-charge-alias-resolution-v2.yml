name: Patch legado de alias de cobrança

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Informar estado
        run: echo "A correção de alias de cobrança já foi incorporada ao código principal e ao backend. Nenhum patch adicional é necessário."
