name: Legacy backend hardening 0.4.13 (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy patch workflow retired. Use validate-current-hardening.yml for the current branch state."
